export interface Task {
  id: number;
  summary: string;
  acceptanceCriteria: string;
  status: string;
  priority: number;
  dueDate: string;
  projectSequence: string;
  projectIdentifier: string;
}
